server:
  port: 8081

spring:
  application:
    name: payments-service
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

products:
  service:
    base-url: http://localhost:8080
